[project]
name = "hhx-backend"
version = "0.1.0"
description = "HHX English Math Classroom API"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "httpx>=0.28.0",
    "openpyxl>=3.1.5",
    "python-multipart>=0.0.20",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
