[project]
name = "hhx-backend"
version = "0.1.0"
description = "HHX English Math Classroom API"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "openpyxl>=3.1.5",
    "pillow>=11.0.0",
    "python-multipart>=0.0.20",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
